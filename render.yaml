services:
  - type: static
    name: sitecass
    buildCommand: echo "Static site - no build needed"
    staticPublishPath: .
    routes:
      - path: "/"
        methods:
          - GET
        missingRoutePolicy: default_index
